import SwiftUI
import Combine

struct StatsView: View {
    
    var body: some View {
        
        NavigationStack {
            
            Text("Stats")
                .font(.largeTitle)
                .navigationTitle("Statistics")
        }
    }
}
